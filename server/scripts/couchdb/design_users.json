{
   "_id": "_design/users",
   "language": "javascript",
   "views": {
       "allUsers": {
           "map": "function(doc) {\n  if (doc.resource && doc.resource === 'User') {\n    emit(doc._id, doc);\n  }\n}"
       },
       "userByLoginTypeAndLoginID": {
           "map": "function(doc) {\n  if (doc.resource && doc.resource === 'User') {\n    emit({ \n      'loginType': doc.loginType,\n      'loginID': doc.loginID\n    }, doc);\n  }\n}"
       }
   }
}